# Yii2 Model Helper

Extensión profesional para Yii2 que facilita el manejo de **múltiples modelos dinámicos** (Dynamic Forms, formularios tabulares, hasMany).

Este helper reemplaza y mejora los métodos tradicionales (`createMultiple`) evitando errores comunes como:

- IDs vacíos
- `array_combine()` fallando
- Problemas al crear y actualizar modelos dinámicos

---

## 🚀 Instalación

```bash
composer require jiuly256/yii2-model-helper

